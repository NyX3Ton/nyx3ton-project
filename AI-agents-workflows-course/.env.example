# Optional local OpenVINO GenAI model directory
OPENVINO_MODEL_DIR=
OPENVINO_DEVICE=CPU

# Email configuration - required only if you want the Email Agent to actually send
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
PREDEFINED_RECIPIENT=

# Gradio
GRADIO_SERVER_NAME=127.0.0.1
GRADIO_SERVER_PORT=7861
